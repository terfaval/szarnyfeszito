# T001 – Project Scaffold & Baseline Setup

## Goal
Clean Next.js + TypeScript baseline with strict linting, env configuration, and folder structure.

## Scope
- App Router baseline verified
- TypeScript strict mode enabled
- ESLint configured (and Prettier if used)
- .env.example created
- Base folders:
  - /app
  - /lib
  - /server
  - /components
  - /types

## Acceptance Criteria
- `npm run build` passes
- `npm run lint` passes
- No hardcoded secrets
